export declare class MdcCardTitle {
    class: string;
    title: string;
    subtitle: string;
    large: boolean;
    private showTitle;
    private showSubtitle;
    private showLarge;
    private elementPrimary;
    private isAvatar;
    private attached();
    private titleChanged(newValue);
    private subtitleChanged(newValue);
    private largeChanged(newValue);
}
