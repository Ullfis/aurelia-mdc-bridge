export declare class MdcRipple {
    private element;
    unbounded: boolean;
    private mdcRipple;
    constructor(element: Element);
    private bind();
    private unbind();
    private attached();
    private detached();
    private unboundedChanged(newValue);
}
