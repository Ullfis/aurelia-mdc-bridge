template

  // SAMPLE
  mdc-grid
    mdc-grid-cell(span="12")
      h4 Select Menus

    // CONFIG AREA
    mdc-grid-cell(span="4")
      button(mdc-button="raised: true;" click.delegate="addRow()") Add option
      br
      mdc-checkbox(checked.bind="disabled") disabled
      br
      mdc-checkbox(checked.bind="box") box
      br
      mdc-text-field(value.bind="labelText") label-text
      br
      mdc-list(dense="true")
        mdc-list-item
          a(href="#icons") Icons
        mdc-list-item
          a(href="#disableditem") Disabled item          
        mdc-list-item
          a(href="#divider") Divider
        mdc-list-item
          a(href="#event") Event
        mdc-list-item
          a(href="#customcolors") Custom colors

    // DEMO AREA
    mdc-grid-cell(span="8")
      section.components
        h6 Example
        br
        mdc-select(value.bind="selectedProductId"
                   box.bind="box" 
                   disabled.bind="disabled" 
                   on-change.delegate="onChange($event)"
                   label-text.bind="labelText")
          mdc-list-item(repeat.for="product of products" model.bind="product.id")
            span ${product.id} - ${product.name}
          mdc-list-item(model="998" disabled="true")
            span 998 - Disabled
          mdc-list-divider
          mdc-list-item(model="999")
            span Something

        br
        style.
          .my-cont .mdc-select {
            min-width: 250px;
          }
          .my-cont .mdc-select .mdc-list-item {
            min-width: 250px;
          }
        mdc-select.my-cont(value.bind="selectedProductId" 
                           disabled.bind="disabled" 
                           on-change.delegate="onChange($event)"
                           label-text.bind="labelText")
          mdc-list-item(repeat.for="product of products" model.bind="product.id")
            span ${product.id} - ${product.name}
            span(slot="start")
              i.material-icons(aria-hidden="true") ${product.icon}
          mdc-list-divider
          mdc-list-item(model="998" disabled="true")
            span 998 - Disabled
          mdc-list-item(model="999")
            span Something
            span(slot="start")
              i.material-icons(aria-hidden="true") gps_fixed
        br
      span Selected product Id: ${selectedProductId}

      section.components
        h6 Custom colors
        br
        mdc-select.my-custom-select(value.bind="selectedProductId" 
                   disabled.bind="disabled" 
                   on-change.delegate="onChange($event)"
                   label-text.bind="labelText")
          mdc-list-item(repeat.for="product of products" model.bind="product.id")
            span ${product.id} - ${product.name}
          mdc-list-item(model="998" disabled="true")
            span 998 - Disabled
          mdc-list-divider
          mdc-list-item(model="999")
            span Something


    // LINKS
    mdc-grid-cell(span="12")
      hr
      a(href="https://material.io/components/web/catalog/input-controls/select-menus/" target="_blank" data-analytics-category="external" data-analytics-action="mdc-select-menus") Material components documentation
      br
      a(href="https://material.io/guidelines/components/text-fields.html" target="_blank" data-analytics-category="external" data-analytics-action="mdg-textfield") Material design guidelines text fields
      br
      a(href="https://material.io/guidelines/components/menus.html" target="_blank" data-analytics-category="external" data-analytics-action="mdg-menus") Material design guidelines menus
      br
      a(href="https://material.io/icons/" target="_blank" data-analytics-category="external" data-analytics-action="mdc-icons") Icons    
      hr    

    // EXAMPLE
    mdc-grid-cell(span="12")
      pre
        code-prism(language="typescript").
          private products = [
            { id: 0, name: 'WiFi' },
            { id: 1, name: 'Bluetooth' },
            { id: 2, name: 'USB' },
          ];

          private selectedProductId = 1;
      pre
        code-prism(language="html").
          &lt;mdc-select value.bind="selectedProductId" disabled.bind="disabled" box.bind="box"&gt;
            &lt;mdc-list-item
              repeat.for="product of products"
              model.bind="product.id"&gt;${product.id} - ${product.name}
            &lt;/mdc-list-item&gt;
          &lt;/mdc-select&gt;  


    // ICONS
    mdc-grid-cell(span="12")
      section(id="icons")
        h5 Icons
        pre
          code-prism.
            &lt;span slot="start"&gt;
              &lt;i class="material-icons" aria-hidden="true"&gt;icon_name&lt;/i&gt;
            &lt;/span&gt;
        pre
          code-prism(language="typescript").
            private products = [
              { id: 0, name: 'WiFi', icon: 'network_wifi' },
              { id: 1, name: 'Bluetooth', icon: 'bluetooth' },
              { id: 2, name: 'USB', icon: 'usb' },
            ];

            private selectedProductId = 1;
        pre
          code-prism(language="html").
            &lt;mdc-select value.bind="selectedProductId"&gt;
              &lt;mdc-list-item
                repeat.for="product of products"
                model.bind="product.id"&gt;
                &lt;span&gt;${product.id} - ${product.name}&lt;/span&gt;
                &lt;span slot="start"&gt;
                  &lt;i class="material-icons" aria-hidden="true"&gt;${product.icon}&lt;/i&gt;
                &lt;/span&gt;
              &lt;/mdc-list-item&gt;
            &lt;/mdc-select&gt;  


    // DISABLE ITEM
    mdc-grid-cell(span="12")
      section(id="disableditem")
        h5 Disabled item
        pre
          code-prism.
            ...
            &lt;mdc-list-item
              disabled="true"
              ...&gt;
        pre


    // DIVIDER
    mdc-grid-cell(span="12")
      section(id="divider")
        h5 Divider
        pre
          code-prism(language="html").
            &lt;mdc-list-divider&gt;&lt;/mdc-list-divider&gt;
        pre
          code-prism(language="html").
            &lt;mdc-select value.bind="value"&gt;
              &lt;mdc-list-item model="1"&gt;Item 1&lt;/mdc-list-item&gt;
              &lt;mdc-list-item model="2"&gt;Item 2&lt;/mdc-list-item&gt;
              &lt;mdc-list-divider&gt;&lt;/mdc-list-divider&gt;
              &lt;mdc-list-item model="3"&gt;Item 3&lt;/mdc-list-item&gt;
            &lt;/mdc-select&gt;  


    // EVENT
    mdc-grid-cell(span="12")
      section(id="event")
        h5 Event
        pre
          code-prism(language="typescript").
            import { IMdcSelectChangeEvent } from 'aurelia-mdc-bridge';
            
            private onChange(e: IMdcSelectChangeEvent) {
              // e.detail: MDCSelect
            }
        pre
          code-prism(language="html").
            &lt;mdc-select on-change.delegate="onChange($event)" ... &gt;
              ...
            &lt;/mdc-select&gt;

    // CUSTOM WIDTH
    mdc-grid-cell(span="12")
      section(id="event")
        h5 Custom Width
        style.
          .my-select-container .mdc-select {
            min-width: 350px;
          }
          .my-select-container .mdc-select .mdc-list-item {
            min-width: 350px;
          }          
        mdc-select.my-select-container
          mdc-list-item Item 1
          mdc-list-item Item 2
        pre
          code-prism(language="css").
            .my-select-container .mdc-select {
              min-width: 350px;
            }
            .my-select-container .mdc-select .mdc-list-item {
              min-width: 350px;
            }
        pre
          code-prism(language="html").
            &lt;mdc-select class="my-select-container" ... &gt;
              ...
            &lt;/mdc-select&gt;

    // CUSTOM COLORS
    mdc-grid-cell(span="12")
      section(id="customcolors")
        h5 Custom Colors
        mdc-select.my-custom-select(value.bind="selectedProductId" 
                   disabled.bind="disabled" 
                   on-change.delegate="onChange($event)"
                   label-text.bind="labelText")
          mdc-list-item(repeat.for="product of products" model.bind="product.id")
            span ${product.id} - ${product.name}
          mdc-list-item(model="998" disabled="true")
            span 998 - Disabled
          mdc-list-divider
          mdc-list-item(model="999")
            span Something
        pre
          code-prism(language="css").
            .my-custom-select {
              @include mdc-select-ink-color(green);
              @include mdc-select-container-fill-color(rgba(red, .1));
              @include mdc-select-label-color(orange);
              @include mdc-select-focused-label-color(blue);
              @include mdc-select-bottom-line-color(black);
              @include mdc-select-focused-bottom-line-color(brown);
            }         
        pre
          code-prism(language="html").
            &lt;mdc-select class="my-custom-select" ... &gt;
              ...
            &lt;/mdc-select&gt;
