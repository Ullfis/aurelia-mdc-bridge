template

  // SAMPLE
  mdc-grid
    mdc-grid-cell(span="12")
      h4 Ripples
      p.
        MDC Ripple provides the Javascript and CSS required to provide components (or any element at all) 
        with a material “ink ripple” interaction effect. It is designed to be efficient, uninvasive, and 
        usable without adding any extra DOM to your elements.

    // LINKS
    mdc-grid-cell(span="12")
      hr
      a(href="https://material.io/components/web/catalog/ripples/" target="_blank" data-analytics-category="external" data-analytics-action="mdc-ripple") Material components documentation
      hr

    // EXAMPLE
    mdc-grid-cell(span="12")
      pre
        code-prism(language="css").
          .my-custom-ripple-target {
            @include mdc-elevation(2);
            height: 100px;
            width: 100px;
            padding: 10px;
          }

    mdc-grid-cell(span="8")
      pre
        code-prism.
          &lt;div class="my-custom-ripple-target" mdc-ripple&gt;&lt;/div&gt;
      pre
        code-prism.
          &lt;div class="my-custom-ripple-target mdc-ripple-surface"&gt;CSS-Only&lt;/div&gt;

    mdc-grid-cell(span="2")
      .my-custom-ripple-target(mdc-ripple)
    mdc-grid-cell(span="2")
      .my-custom-ripple-target.mdc-ripple-surface CSS-Only

    // UNBOUNDED
    mdc-grid-cell(span="8")
      h5 unbounded
      p Unbounded ripples. such as those used for checkboxes and radio buttons:
      pre
        code-prism.
          &lt;div class="my-custom-ripple-target" mdc-ripple="unbounded: true;"&gt;&lt;/div&gt;
      pre
        code-prism.
          &lt;div class="my-custom-ripple-target mdc-ripple-surface" data-mdc-ripple-is-unbounded&gt;CSS-Only&lt;/div&gt;

    mdc-grid-cell(span="2")
      .my-custom-ripple-target(mdc-ripple="unbounded: true;")
    mdc-grid-cell(span="2")
      .my-custom-ripple-target.mdc-ripple-surface(data-mdc-ripple-is-unbounded) CSS-Only
      

    // ACCENT
    mdc-grid-cell(span="8")
      h5 accent
      p Styling ripple surface using the configured theme’s accent color:
      pre
        code-prism.
          &lt;div class="my-custom-ripple-target" mdc-ripple="accent: true;"&gt;&lt;/div&gt;
      pre
        code-prism.
          &lt;div class="my-custom-ripple-target mdc-ripple-surface mdc-ripple-surface--accent"&gt;CSS-Only&lt;/div&gt;

    mdc-grid-cell(span="2")
      .my-custom-ripple-target(mdc-ripple="accent: true;")
    mdc-grid-cell(span="2")
      .my-custom-ripple-target.mdc-ripple-surface.mdc-ripple-surface--accent CSS-Only

    // PRIMARY
    mdc-grid-cell(span="8")
      h5 primary
      p Styling ripple surface using the configured theme’s primary color:
      pre
        code-prism.
          &lt;div class="my-custom-ripple-target" mdc-ripple="primary: true;"&gt;&lt;/div&gt;
      pre
        code-prism.
          &lt;div class="my-custom-ripple-target mdc-ripple-surface mdc-ripple-surface--primary"&gt;CSS-Only&lt;/div&gt;

    mdc-grid-cell(span="2")
      .my-custom-ripple-target(mdc-ripple="primary: true;")
    mdc-grid-cell(span="2")
      .my-custom-ripple-target.mdc-ripple-surface.mdc-ripple-surface--primary CSS-Only
